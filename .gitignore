# Environment / config — never commit real values
.env
.env.*
!.env.example

# Service account keys — these ARE secret. They belong in GitHub Actions
# secrets, never in the repo.
*serviceAccount*.json
*-firebase-adminsdk-*.json

# Windows folder metadata
desktop.ini
Thumbs.db
.DS_Store

# Local archive of the original template — not part of the site
APOVIX.zip

# Stray extensionless duplicate of assets/img/hero.svg
/hero

# Firebase deploy tooling caches
.firebase/
firebase-debug.log*
firestore-debug.log*

# Editor
.vscode/
.idea/

# Claude Code local settings
.claude/settings.local.json
